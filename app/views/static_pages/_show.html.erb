
<h1>
  <div class="container-fluid">
    <lu class="list-group">
      <li class="img-thumbnail"><%= gravatar_for @user %> </li>
      <li class="list-group-item"><%= @user.first_name %></li>
      <li class="list-group-item"><%= @user.last_name %></li>
      <li class="list-group-item"><%= @user.email%></li>
      <li class="list-group-item"><%= @user.phone%></li>
    </lu>
  </div>
</h1>

    

  <h2>Niños: (<%= @user.offsprings.count %>)</h2>
    
  <h4>Niños cuyo padre o tutor legal es el usuario:</h4>

  <script src="/bower_components/datatables/media/js/jquery.dataTables.js"></script>
  <script>
    // Initialize Datatables
    $(document).ready(function() {
      $('.datatable').dataTable();
    });
  </script>
  <% if @user.offsprings.any? %>
   
      <div class="table-responsive">    
        <table id= "offspring_table" class="datatable table table-striped table-bordered table-hover">
          <thead>
            <tr>
              <th>Nombre</th>
              <th>Apellidos</th>
              <th>Curso</th>
              <th>Padre</th>
              <th>Administrar</th>

            </tr>
          </thead>
          <tbody>
              <%= render @user.offsprings %>
          </tbody>
        </table>
      </div>   
    <% end %>
  </div>


<div class="container-fluid">
      <%= link_to "Añadir niño", new_offspring_path, class:"btn btn-lg btn-info" %>
    </div>
<script>
  // Row Checkbox Selection
  $(document).ready(function () {
    $("input[type='checkbox']").change(function (e) {
      if ($(this).is(":checked")) {
        $(this).closest('.list-group-item').addClass("active");
      } else {
        $(this).closest('.list-group-item').removeClass("active");
      }
    });
  });
</script>
